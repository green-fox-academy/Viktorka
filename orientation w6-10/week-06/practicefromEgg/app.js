'use struct';

// egyik file
const coctails = []...
const alcoholList = []...

function getCoctailsByIngredient(alcohol) {
  if (!alcohol) {
    return {alcoholList, coctails};
  } else {
    for
      for

  }
}

module.exports = getCoctailsByIngredient;

// app.js

const getCoctailsByIngredient = require('masik file')

app.get('/', (req, res) => {
  res.render('home', getCoctailsByIngredient(req.query.alcohol))
})

//
if (process.argv[3] == '-l')
console.log(getCoctailsByIngredient(process.argv[4]))
